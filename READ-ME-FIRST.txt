CV REWRITE - UPDATE (clearer flagged items)

This makes the "flagged gaps" step easy to get past:
- A big "Leave them all out and continue" button is now the main action.
- Each flagged item is explained in plain English ("We listed X as a skill but
  couldn't confirm it - have you used it? If not, leave it out.").
- You can't accidentally reach the pay step with items outstanding.

TO APPLY: drag lib, public and server.js into your cv-rewrite-app folder
(Replace), then GitHub Desktop: Commit to main, Push origin. Wait for Live.

(Reminder: set CV_PRICE_GBP to 5.00 in Render if you haven't.)
