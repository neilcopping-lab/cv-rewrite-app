CV REWRITE - FIX + FEATURES UPDATE

FIXES
- "Leave them all out and continue" no longer gets stuck. It now clears the
  flagged items WITHOUT re-running the AI and takes you straight to the design
  step. (Previously it regenerated, which could surface new flags in a loop.)
- Font sizes now match the main the-common-people.com site (16px body,
  44px H1, 30px H2, 12px eyebrows, matching buttons).

FEATURES (from this round)
- New "Creative / Portfolio" design with a clickable links block (all users).
- Clickable links on every design (human version); ATS keeps plain URLs.
- Optional photo (human CV only, never on the ATS version).
- Optional links (LinkedIn / portfolio / website / GitHub) on step 1.
- Regeneration cap (REGEN_CAP, default 8) to protect your AI costs.

APPLY: replace lib, public, server.js, render.yaml, DEPLOY.md in your
cv-rewrite-app folder (Replace) -> GitHub Desktop: Commit -> Push.
(Accounts still need the Render disk + DB_PATH + Resend - see DEPLOY.md.)
